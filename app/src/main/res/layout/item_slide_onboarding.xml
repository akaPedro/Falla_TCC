<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:paddingHorizontal="40dp"
    android:paddingVertical="24dp">

    <androidx.cardview.widget.CardView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginBottom="40dp"
        app:cardCornerRadius="60dp"
        app:cardBackgroundColor="@color/verde_fosco_header"
        app:cardElevation="6dp">

        <ImageView
            android:id="@+id/img_slide_icone"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_gravity="center"
            android:scaleType="fitCenter"
            app:tint="@color/texto_header"/>

    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/txt_slide_titulo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/texto_principal"
        android:layout_marginBottom="20dp"/>

    <TextView
        android:id="@+id/txt_slide_descricao"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="16sp"
        android:textColor="@color/texto_principal"
        android:alpha="0.75"
        android:lineSpacingMultiplier="1.4"
        android:layout_marginBottom="24dp"/>

    <!-- Campo de nome — visível apenas no slide de perfil -->
    <androidx.cardview.widget.CardView
        android:id="@+id/container_campo_nome"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:cardCornerRadius="12dp"
        app:cardBackgroundColor="@color/fundo_card_padrao"
        app:cardElevation="2dp">

        <EditText
            android:id="@+id/edt_nome_onboarding"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Digite o nome aqui"
            android:textSize="18sp"
            android:gravity="center"
            android:padding="16dp"
            android:background="@null"
            android:inputType="textPersonName|textCapWords"
            android:textColor="@color/texto_principal"
            android:textColorHint="#8A92A3"/>

    </androidx.cardview.widget.CardView>

</LinearLayout>